spring.application.name=planilla-backend

# Server port — private range to avoid conflicts with common dev services
server.port=49301

# Faster startup: delay bean creation until first use
spring.main.lazy-initialization=true

# PostgreSQL — remote DB, used only for stored procedure execution
postgres.datasource.url=jdbc:postgresql://192.168.0.20:5432/sunhive
postgres.datasource.username=@POSTGRES_DB_USERNAME@
postgres.datasource.password=@POSTGRES_DB_PASSWORD@
postgres.datasource.driver-class-name=org.postgresql.Driver
postgres.datasource.hikari.maximum-pool-size=5
postgres.datasource.hikari.minimum-idle=1
postgres.datasource.hikari.connection-timeout=10000
postgres.datasource.hikari.idle-timeout=300000

# H2 — local file DB, used for duplicate detection log
h2.datasource.url=jdbc:h2:file:${user.home}/.planilla/data/planilla-log
h2.datasource.username=sa
h2.datasource.password=
h2.datasource.driver-class-name=org.h2.Driver

# Async job processing
job.max-retries=3

# File upload limits
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

holiday.api.url=https://date.nager.at/api/v3/PublicHolidays/{year}/GT
holiday.api.timeout-seconds=3

# CORS allowed origins — production Tauri origins; dev profile adds the Vite dev server.
# Windows WebView2 uses http://tauri.localhost; macOS/Linux use tauri://localhost.
cors.allowed-origins=tauri://localhost,http://tauri.localhost,https://tauri.localhost

# Graceful shutdown — drain in-flight requests before Spring tears down beans
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s
